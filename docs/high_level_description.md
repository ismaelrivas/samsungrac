# Climate IP Integration

The `climate_ip` integration allows you to control Samsung Air Conditioners via local network (IP). It supports legacy 2878 port devices and modern 8888 port devices via REST APIs and Raw sockets. It features auto-discovery, robust fallback algorithms for unreachable nodes, and optimistic state predictions for instant UI feedback.
